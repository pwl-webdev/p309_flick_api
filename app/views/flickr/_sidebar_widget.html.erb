<div class="flickr">
  <ul>
    <% photos.each do |row| %>
      <li>
          <%= link_to(image_tag(FlickRaw.url_b(flickr.photos.getInfo(:photo_id => row.id)), :class => 'flickr_photo', :title => row.title, :border => 0, :size => '75x75')) %>
      </li>
    <% end %>
  </ul>
</div>